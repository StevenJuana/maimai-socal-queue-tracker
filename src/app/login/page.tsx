import Link from "next/link";
import { Navigation } from "@/components/Navigation";
import { loginAction } from "@/lib/actions";
export default async function Login({ searchParams }: { searchParams: Promise<{error?:string;message?:string}> }) { const {error,message}=await searchParams; return <main className="site-shell"><Navigation/><h1 className="page-title">Welcome back</h1><p className="page-subtitle">Log in to update a location or check your player verification.</p>{error&&<div className="notice error" role="alert">{error === "setup" ? "Connect Supabase first by adding the environment values from .env.example." : error}</div>}{message&&<div className="notice success">{message.replaceAll("+"," ")}</div>}<form action={loginAction} className="form-stack"><div className="field"><label htmlFor="username">Username</label><input id="username" type="text" name="username" required maxLength={255} autoComplete="username"/></div><div className="field"><label htmlFor="password">Password</label><input id="password" type="password" name="password" required autoComplete="current-password"/></div><button className="primary-button">Log in</button></form><p className="auth-switch">New here? <Link href="/signup">Create an account</Link></p></main>; }
