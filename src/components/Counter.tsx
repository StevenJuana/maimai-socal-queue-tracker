"use client";
export function Counter({ name, label, help }: { name: string; label: string; help: string }) {
  return <div className="stepper-row"><div className="stepper-label">{label}<small>{help}</small></div><div className="stepper"><button type="button" aria-label={`Decrease ${label}`} onClick={(e) => { const row=e.currentTarget.parentElement; const out = row?.querySelector("output"); const input=row?.querySelector("input"); if (out&&input) { const next=String(Math.max(0, Number(out.value) - 1)); out.value=next; input.value=next; } }}>−</button><output aria-live="polite">0</output><input type="hidden" name={name} value="0"/><button type="button" aria-label={`Increase ${label}`} onClick={(e) => { const row = e.currentTarget.parentElement; const out = row?.querySelector("output"); const input = row?.querySelector("input"); if (out && input) { const next = Math.min(99, Number(out.value) + 1); out.value = String(next); input.value = String(next); } }}>+</button></div></div>;
}
